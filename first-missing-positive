num = set(nums)
        if len(nums) == 1 :
            if nums[0] < 1 :
                return 1
                
        for i in range(1, len(nums)+1):
            if i not in num:
                return i
                break
        return len(nums)+1       
