class Solution:
    def minPatches(self, nums: List[int], n: int) -> int:
        miss = 1
        patch = 0
        for num in nums:
            if num <= miss:
                miss +=num
            elif num > miss:
                patch+=1
                miss = 2 * miss
        return patch        
