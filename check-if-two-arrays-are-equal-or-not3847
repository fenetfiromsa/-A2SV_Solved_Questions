class Solution:
    def checkEqual(self, a, b) -> bool:
        a.sort()
        b.sort()
        n=len(a)
        for i in range(n):
            if a[i]!=b[i]:
                return False
            
        return True
            
        
