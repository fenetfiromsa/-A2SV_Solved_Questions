class Solution:
    def majorityElement(self, nums: List[int]) -> List[int]:
        count = Counter(nums)
        res = []
        n = len(nums)
        for num, fre in count.items():
            if fre > n/3:
                res.append(num)
        return res   
