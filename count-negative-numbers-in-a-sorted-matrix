class Solution:
    def countNegatives(self, grid: List[List[int]]) -> int:
        row=len(grid)
        col=len(grid[0])
        ans=0
        for i in range(row):
            for j in range(col):
                if int(grid[i][j])<0:
                    ans+=1
        return ans            

        
