class Solution:
    def customSortString(self, order: str, s: str) -> str:
        res = ""
        for i in order:
            if i in s:
                res+=i * s.count(i)
                
        for i in s:
            if i not in order :
                res+=i
        return res                

        
